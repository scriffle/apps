<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Topic — Key Points (Tabbed Slides)</title>
  <style>
    :root{
      --fs:1;
      --bg:#ffffff; --fg:#0b1b2b; --muted:#c7d2fe; --accent:#10b981; --accent-2:#059669; --card:#f8fafc;
      --shadow:0 10px 30px rgba(0,0,0,.08);
    }
    .dark{ --bg:#0b1220; --fg:#e5f0ff; --muted:#1f2a44; --accent:#34d399; --accent-2:#6ee7b7; --card:#0f172a; --shadow:0 16px 40px rgba(0,0,0,.55); }
    html,body{height:100%}
    body{margin:0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji"; background:var(--bg); color:var(--fg);}
    .wrap{max-width:1100px; margin:0 auto; padding:16px;}
    header{display:flex; align-items:center; gap:12px; justify-content:space-between; position:sticky; top:0; background:linear-gradient(180deg,var(--bg) 0%, rgba(0,0,0,0) 100%); backdrop-filter:saturate(120%) blur(2px); z-index:10;}
    .brand{display:flex; align-items:center; gap:10px}
    .logo{width:36px; height:36px; border-radius:12px; background:linear-gradient(135deg,var(--accent), var(--accent-2)); box-shadow: var(--shadow);}
    .h1{font-size: clamp(20px, 3vw, 28px); font-weight:800; letter-spacing:.2px}
    .controls{display:flex; gap:8px; align-items:center}
    button{cursor:pointer; border:1px solid transparent; border-radius:999px; padding:8px 12px; font-weight:600; background:var(--accent); color:white; box-shadow:var(--shadow);}
    button.secondary{background:transparent; color:var(--fg); border-color:var(--muted)}
    button.ghost{background:transparent; color:var(--fg); border-color:transparent}
    button.mini{padding:6px 8px; font-size:.9em}
    kbd{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace; background:var(--card); border:1px solid var(--muted); border-bottom-width:2px; border-radius:6px; padding:1px 6px; font-size:.85em}

    .tabs{display:flex; gap:8px; overflow:auto; padding:10px 2px 14px; border-bottom:1px solid var(--muted); scrollbar-width:thin}
    .tabs button{background:transparent; color:var(--fg); border-color:var(--muted)}
    .tabs button.active{background:var(--accent); color:white; border-color:transparent}

    .stage{position:relative;}
    .slide{display:none; background:var(--card); border:1px solid var(--muted); border-radius:20px; padding:28px; margin:16px 0; box-shadow:var(--shadow);}
    .slide.active{display:block; animation:fade .25s ease-out}
    @keyframes fade{from{opacity:0; transform:translateY(4px)} to{opacity:1; transform:none}}

    h2{font-size: calc(var(--fs) * (clamp(22px, 2.6vw, 32px))); margin:0 0 12px; line-height:1.15}
    h3{font-size: calc(var(--fs) * (clamp(16px, 1.8vw, 22px))); margin:18px 0 8px}
    p{font-size: calc(var(--fs) * (clamp(15px, 1.2vw, 18px))); line-height:1.55}
    ul,ol{padding-left:22px; margin:10px 0}
    li{margin:6px 0; font-size: calc(var(--fs) * 1em)}
    .grid-2{display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:18px}
    .callout{border-left:6px solid var(--accent); padding:10px 12px; background:linear-gradient(180deg, rgba(16,185,129,.08), transparent); border-radius:12px; margin:12px 0}
    .eq{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace; background:rgba(16,185,129,.06); padding:2px 6px; border-radius:8px}
    .muted{opacity:.85}
    .progress{font-weight:700}
    .mini{font-size:.9em}
    .footer{display:flex; justify-content:space-between; align-items:center; gap:8px; margin:8px 0 24px}
    .pill{display:inline-flex; align-items:center; gap:6px; padding:6px 10px; border-radius:999px; border:1px solid var(--muted);}

    details{background:rgba(16,185,129,.08); border:1px solid var(--muted); border-radius:12px; padding:10px 12px; margin:8px 0}
    summary{cursor:pointer; font-weight:700}

    /* Presenter mode */
    .present header, .present .tabs, .present .footer .shortcuts{display:none}
    .present .wrap{max-width:none; padding:24px 48px; height:100vh; display:flex; flex-direction:column;}
    .present .stage{flex:1; display:flex; align-items:center; justify-content:center;}
    .present .slide.active{
      min-height:auto; 
      height:auto;
      max-height:85vh;
      width:100%;
      max-width:1400px;
      font-size: calc(var(--fs) * 1.3em);
      padding:40px 48px;
      display:flex;
      flex-direction:column;
      justify-content:center;
      overflow-y:auto;
    }
    .present .footer{margin-top:auto; padding-top:16px;}
    .slide-hints{display:none; font-size:11px; opacity:0.6; color:var(--fg); margin-left:16px}
    .present .slide-hints{display:inline}
    .slide-hint-link{cursor:pointer !important; text-decoration:none; color:inherit; user-select:none}
    .slide-hint-link:hover{opacity:1; text-decoration:underline}

    @media print{
      header,.tabs,.footer{display:none !important}
      .slide{display:block !important; break-after:page; box-shadow:none; border:0; background:white}
      body{background:white}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <div class="brand" aria-label="App title">
        <div class="logo" aria-hidden="true"></div>
        <div>
          <div class="h1">Your Topic Title — Key Points</div>
          <div class="muted mini"><span id="count"></span> slides</div>
        </div>
      </div>
      <div class="controls">
        <button class="secondary" id="prev" title="Previous slide (←)">◀︎</button>
        <button class="secondary" id="next" title="Next slide (→)">▶︎</button>
        <button class="secondary" id="theme" title="Toggle light/dark (T)">🌗</button>
        <button class="secondary" id="present" title="Presenter mode (P)">🖥️</button>
        <button class="secondary" id="print" title="Print/Save PDF">🖨️</button>
      </div>
    </header>

    <nav class="tabs" aria-label="Slides">
    </nav>

    <div class="stage" id="stage">
      <!-- SLIDE 1: Introduction -->
      <section class="slide" data-title="Introduction">
        <h2>Slide 1 Title — Introduction</h2>
        <div class="grid-2">
          <div>
            <h3>Section A</h3>
            <ul>
              <li>Point one about your topic</li>
              <li>Point two with details</li>
              <li>Point three</li>
              <li>Additional information</li>
            </ul>
          </div>
          <div>
            <h3>Section B</h3>
            <ul>
              <li>Another key concept</li>
              <li>Supporting detail</li>
              <li>More information</li>
            </ul>
          </div>
        </div>
        <div class="callout"><strong>Tip:</strong> Add important notes or key takeaways in callout boxes like this</div>
      </section>

      <!-- SLIDE 2: Main Concept -->
      <section class="slide" data-title="Main Concept">
        <h2>Slide 2 Title — Main Concept</h2>
        <div class="grid-2">
          <div>
            <h3>Left Column</h3>
            <ul>
              <li>First point</li>
              <li>Second point</li>
              <li>Examples: item A, item B, item C</li>
            </ul>
          </div>
          <div>
            <h3>Right Column</h3>
            <ul>
              <li>Contrasting point</li>
              <li>Different perspective</li>
              <li>Examples: item X, item Y, item Z</li>
            </ul>
          </div>
        </div>
        <div class="callout"><strong>Note:</strong> Use callouts to highlight tricky concepts or edge cases</div>
        <h3>Additional Context</h3>
        <p>Add explanatory paragraphs here when you need more detail than bullet points.</p>
      </section>

      <!-- SLIDE 3: Key Principles -->
      <section class="slide" data-title="Key Principles">
        <h2>Slide 3 Title — Key Principles</h2>
        <p>Introduction paragraph explaining the upcoming list.</p>
        <ol>
          <li><strong>First principle</strong> — explanation of what this means</li>
          <li><strong>Second principle</strong> — more details here</li>
          <li><strong>Third principle</strong> — additional context</li>
        </ol>
        <div class="callout">Emphasize the most important principle or concept</div>
        <h3>Background Information</h3>
        <p>Provide historical context, examples, or additional explanation as needed.</p>
      </section>

      <!-- SLIDE 4: Deep Dive -->
      <section class="slide" data-title="Deep Dive">
        <h2>Slide 4 Title — Deep Dive</h2>
        <p>Opening statement about this detailed topic:</p>
        <div class="grid-2">
          <div>
            <h3>Aspect 1</h3>
            <p>Description and explanation</p>
            <h3>Aspect 2</h3>
            <p>More description and details</p>
          </div>
          <div>
            <h3>Aspect 3</h3>
            <p>Additional information</p>
            <h3>Aspect 4</h3>
            <p>Final details</p>
          </div>
        </div>
        <div class="callout">Highlight the connection between all these aspects</div>
      </section>

      <!-- SLIDE 5: Detailed Topic A -->
      <section class="slide" data-title="Topic A Details">
        <h2>Slide 5 — Topic A in Detail</h2>
        <h3>Function</h3>
        <p>Explain what this is and why it matters</p>
        <h3>Key Points</h3>
        <ul>
          <li>First important point</li>
          <li>Second important point</li>
          <li>Third important point</li>
          <li>Supporting detail</li>
          <li>Additional context</li>
        </ul>
        <div class="callout"><strong>Why it matters:</strong> Explain the significance or practical application</div>
      </section>

      <!-- SLIDE 6: Detailed Topic B -->
      <section class="slide" data-title="Topic B Details">
        <h2>Slide 6 — Topic B in Detail</h2>
        <h3>Function</h3>
        <p>Explain this concept clearly</p>
        <h3>Composition</h3>
        <ul>
          <li>Component A</li>
          <li>Component B</li>
          <li>Component C</li>
        </ul>
        <h3>Key Points</h3>
        <ul>
          <li>Main characteristic</li>
          <li>Important property</li>
          <li>Critical distinction</li>
          <li>Additional note</li>
        </ul>
        <div class="callout"><strong>Think of it as:</strong> Use an analogy or metaphor to make it memorable</div>
      </section>

      <!-- SLIDE 7: Detailed Topic C -->
      <section class="slide" data-title="Topic C Details">
        <h2>Slide 7 — Topic C in Detail</h2>
        <h3>Function</h3>
        <p>Core function and purpose</p>
        <h3>Structure</h3>
        <ul>
          <li>Structural element 1</li>
          <li>Structural element 2</li>
          <li>How it's organized</li>
        </ul>
        <h3>Location</h3>
        <ul>
          <li>Where you find this</li>
          <li>Alternative locations</li>
          <li>Context for placement</li>
        </ul>
        <div class="callout"><strong>Essential role:</strong> Explain why this is critical or indispensable</div>
      </section>

      <!-- SLIDE 8: Detailed Topic D -->
      <section class="slide" data-title="Topic D Details">
        <h2>Slide 8 — Topic D in Detail</h2>
        <h3>Function</h3>
        <p>Primary purpose and role</p>
        <h3>Structure</h3>
        <ul>
          <li>Structural component</li>
          <li>How it's built</li>
          <li>Key model or framework</li>
        </ul>
        <h3>Key Roles</h3>
        <ul>
          <li>First major role</li>
          <li>Second major role</li>
          <li>Third major role</li>
          <li>Supporting function</li>
        </ul>
        <div class="callout"><strong>Critical feature:</strong> Highlight what makes this special or unique</div>
      </section>

      <!-- SLIDE 9: Comparison -->
      <section class="slide" data-title="Comparison">
        <h2>Slide 9 — Comparing Types</h2>
        <div class="grid-2">
          <div>
            <h3>Type A</h3>
            <ul>
              <li>Example: specific instance</li>
              <li>Characteristic 1</li>
              <li>Characteristic 2</li>
              <li>How it differs</li>
              <li>Common thread</li>
            </ul>
          </div>
          <div>
            <h3>Type B</h3>
            <ul>
              <li>Examples: other instances</li>
              <li>Different characteristic 1</li>
              <li>Different characteristic 2</li>
              <li>Complexity level</li>
              <li>Shared common thread</li>
            </ul>
          </div>
        </div>
        <div class="callout"><strong>Universal truth:</strong> Despite differences, emphasize what all types share</div>
      </section>

      <!-- SLIDE 10: Check Questions -->
      <section class="slide" data-title="Check Questions">
        <h2>Check-in Questions</h2>
        <ol>
          <li>Question 1 about key concept</li>
          <li>Question 2 asking for examples</li>
          <li>Question 3 about function</li>
          <li>Question 4 about main principles</li>
        </ol>
        <details><summary>Reveal Answers</summary>
          <ol>
            <li><strong>Answer 1:</strong> Explanation here</li>
            <li><strong>Answer 2:</strong> List examples or explanation</li>
            <li><strong>Answer 3:</strong> Function description</li>
            <li><strong>Answer 4:</strong> List or explain principles</li>
          </ol>
        </details>
      </section>

      <!-- SLIDE 11: Practice Questions -->
      <section class="slide" data-title="Practice Questions">
        <h2>Practice Questions</h2>
        <ol>
          <li>Recall the main elements</li>
          <li>State the function of each element</li>
          <li>Define key term</li>
          <li>Compare two concepts</li>
        </ol>
        <details><summary>Complete Answers</summary>
          <ol>
            <li><strong>Main elements:</strong> List them here</li>
            <li><strong>Functions:</strong>
              <ul>
                <li>Element 1: its function</li>
                <li>Element 2: its function</li>
                <li>Element 3: its function</li>
                <li>Element 4: its function</li>
              </ul>
            </li>
            <li><strong>Definition:</strong> Clear definition here</li>
            <li><strong>Comparison:</strong> Explain similarities and differences</li>
          </ol>
        </details>
      </section>

      <!-- SLIDE 12: Application Questions -->
      <section class="slide" data-title="Application Questions">
        <h2>Application Questions</h2>
        <h3>Q5: Context-dependent question</h3>
        <details><summary>Answer & Explanation</summary>
          <p><strong>Answer:</strong> Direct answer</p>
          <p><strong>Explanation:</strong> Detailed explanation of nuances or context</p>
        </details>
        <h3>Q6: Real-world application question</h3>
        <details><summary>Sample Answers</summary>
          <p><strong>Category A examples:</strong> List examples here</p>
          <p><strong>Category B examples:</strong> List different examples here</p>
        </details>
        <h3>Q7: Recall fundamental principle</h3>
        <details><summary>Complete Answer</summary>
          <ol>
            <li>First principle</li>
            <li>Second principle</li>
            <li>Third principle</li>
          </ol>
        </details>
      </section>

      <!-- SLIDE 13: Summary -->
      <section class="slide" data-title="Summary">
        <h2>Summary — Key Takeaways</h2>
        <h3>Big Ideas</h3>
        <ul>
          <li><strong>First big idea</strong> and what it means</li>
          <li><strong>Second big idea</strong> and its importance</li>
          <li><strong>Third big idea</strong> connecting everything</li>
        </ul>
        <h3>The Core Elements</h3>
        <div class="grid-2">
          <div>
            <p class="eq">Element 1 → Purpose</p>
            <p class="eq">Element 2 → Purpose</p>
          </div>
          <div>
            <p class="eq">Element 3 → Purpose</p>
            <p class="eq">Element 4 → Purpose</p>
          </div>
        </div>
        <div class="callout"><strong>Core principle:</strong> State the unifying concept or final takeaway that ties everything together.</div>
      </section>
    </div>

    <div class="footer">
      <div class="pill"><span class="progress" id="progress">1 / 13</span>&nbsp;&nbsp; <button class="secondary mini" id="fsDown" title="Smaller (–)">A−</button> <button class="secondary mini" id="fsUp" title="Larger (+)">A+</button> <span class="shortcuts">• Shortcuts: <kbd>←</kbd>/<kbd>→</kbd>, <kbd>T</kbd>, <kbd>P</kbd>, <kbd>E</kbd></span> <span class="slide-hints" id="slideHints"></span></div>
    </div>
  </div>

  <script>
    const stage = document.getElementById('stage');
    const slides = Array.from(stage.querySelectorAll('.slide'));
    const tabs = document.querySelector('.tabs');
    const prevBtn = document.getElementById('prev');
    const nextBtn = document.getElementById('next');
    const themeBtn = document.getElementById('theme');
    const presentBtn = document.getElementById('present');
    const printBtn = document.getElementById('print');
    const progress = document.getElementById('progress');
    const count = document.getElementById('count');
    const fsDown = document.getElementById('fsDown');
    const fsUp = document.getElementById('fsUp');
    const slideHints = document.getElementById('slideHints');
    count.textContent = slides.length;
    
    let fscale = parseFloat(localStorage.getItem('slides-fs') || '1');
    const applyFs = ()=> document.documentElement.style.setProperty('--fs', String(fscale));
    applyFs();
    if(fsDown) fsDown.addEventListener('click', ()=>{ fscale = Math.max(0.8, +(fscale - 0.1).toFixed(2)); localStorage.setItem('slides-fs', String(fscale)); applyFs(); });
    if(fsUp) fsUp.addEventListener('click', ()=>{ fscale = Math.min(2.5, +(fscale + 0.1).toFixed(2)); localStorage.setItem('slides-fs', String(fscale)); applyFs(); });

    let idx = 0;

    function makeTabs(){
      slides.forEach((s,i)=>{
        const b = document.createElement('button');
        b.setAttribute('type','button');
        b.textContent = `${i+1}. ${s.dataset.title || 'Slide '+(i+1)}`;
        b.addEventListener('click',()=>show(i,true));
        tabs.appendChild(b);
      })
    }

    function show(i, fromUser=false){
      idx = Math.max(0, Math.min(slides.length-1, i));
      slides.forEach((s,j)=>s.classList.toggle('active', j===idx));
      const btns = tabs.querySelectorAll('button');
      btns.forEach((b,j)=>b.classList.toggle('active', j===idx));
      progress.textContent = `${idx+1} / ${slides.length}`;
      document.title = `${idx+1}/${slides.length} — ${slides[idx].dataset.title} — Your Topic`;
      
      // Update presenter hints with clickable links
      slideHints.innerHTML = '';
      let hints = [];
      if(idx > 0){
        const prevLink = document.createElement('span');
        prevLink.className = 'slide-hint-link';
        prevLink.textContent = `◀︎ ${slides[idx-1].dataset.title}`;
        prevLink.addEventListener('click', (e) => {
          e.stopPropagation();
          prev();
        });
        hints.push(prevLink);
      }
      if(idx < slides.length - 1){
        const nextLink = document.createElement('span');
        nextLink.className = 'slide-hint-link';
        nextLink.textContent = `${slides[idx+1].dataset.title} ▶︎`;
        nextLink.addEventListener('click', (e) => {
          e.stopPropagation();
          next();
        });
        hints.push(nextLink);
      }
      if(hints.length > 0){
        slideHints.appendChild(document.createTextNode(' • '));
        hints.forEach((hint, i) => {
          slideHints.appendChild(hint);
          if(i < hints.length - 1){
            slideHints.appendChild(document.createTextNode(' | '));
          }
        });
      }
      
      if(fromUser) {
        try {
          history.replaceState(null,'',`#s${idx+1}`);
        } catch(e) {}
      }
      localStorage.setItem('slides-current', String(idx));
    }

    function next(){ show(idx+1,true) }
    function prev(){ show(idx-1,true) }

    makeTabs();

    const hashMatch = location.hash.match(/#s(\d+)/);
    const saved = localStorage.getItem('slides-current');
    if(hashMatch){ idx = Math.min(slides.length-1, Math.max(0, parseInt(hashMatch[1],10)-1)); }
    else if(saved){ idx = Math.min(slides.length-1, Math.max(0, parseInt(saved,10))); }
    show(idx);

    prevBtn.addEventListener('click', prev);
    nextBtn.addEventListener('click', next);
    themeBtn.addEventListener('click', ()=>{
      document.documentElement.classList.toggle('dark');
      localStorage.setItem('slides-theme', document.documentElement.classList.contains('dark')? 'dark':'light');
    });
    presentBtn.addEventListener('click', ()=>{
      document.body.classList.toggle('present');
    });
    printBtn.addEventListener('click', ()=>window.print());

    const savedTheme = localStorage.getItem('slides-theme');
    if(savedTheme === 'dark') document.documentElement.classList.add('dark');

    window.addEventListener('keydown', (e)=>{
      if(['ArrowRight','PageDown'].includes(e.key)) { e.preventDefault(); next(); }
      if(['ArrowLeft','PageUp'].includes(e.key))   { e.preventDefault(); prev(); }
      if(e.key.toLowerCase()==='t'){ themeBtn.click(); }
      if(e.key.toLowerCase()==='p'){ presentBtn.click(); }
      if(e.key.toLowerCase()==='e'){ 
        document.querySelectorAll('details').forEach(d => d.open = true);
      }
      if(e.key === '+' || e.key === '='){ 
        e.preventDefault(); 
        fsUp.click(); 
      }
      if(e.key === '-'){ 
        e.preventDefault(); 
        fsDown.click(); 
      }
    });

    // Click to advance anywhere on slide (only in normal mode, not present mode)
    stage.addEventListener('click', (e)=>{
      // Ignore clicks in present mode
      if(document.body.classList.contains('present')) {
        e.stopPropagation();
        return;
      }
      // Ignore clicks on interactive elements
      if(e.target.closest('button,details,summary,a,.slide-hint-link')) {
        return;
      }
      next();
    })
  </script>
</body>
</html>